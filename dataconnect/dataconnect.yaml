specVersion: "v1"
serviceId: "pelada-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pelada-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
